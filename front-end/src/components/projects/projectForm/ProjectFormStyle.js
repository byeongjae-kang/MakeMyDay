import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
  field: {
    marginTop: 20,
    marginBottom: 20,
    width: 500,
    display: "block",
  },
  divide: {
    display: "flex",
    justifyContent: "space-between",
    marginTop: 20,
  },
});

export { useStyles };
